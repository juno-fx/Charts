apiVersion: v1
kind: Service
metadata:
  name: {{ include "tranquility.fullname" . }}
  labels:
    {{- include "tranquility.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tranquility.selectorLabels" . | nindent 4 }}
