apiVersion: v1
kind: Service
metadata:
  name: {{ include "workstation.fullname" . }}
  labels:
    {{- include "workstation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "workstation.selectorLabels" . | nindent 4 }}
